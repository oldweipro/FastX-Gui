PluginInterface, #view {
    border: none;
    background-color: transparent;
}

ScrollArea {
    border: none;
    background-color: transparent;
}

/* 左侧面板 - 透明背景 */
#left_panel {
    background-color: transparent;
    border-right: 1px solid rgba(70, 70, 75, 0.4);
}

/* 分割器 */
QSplitter::handle {
    background-color: rgba(70, 70, 75, 0.4);
}

QSplitter::handle:horizontal {
    width: 1px;
}
